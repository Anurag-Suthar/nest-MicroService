import { IsEmail } from 'class-validator';

const ERROR_MESSAGES = {
  CREDENTIALS_NOT_VALID: 'Credentails are not valid!.',
  EMAIL_ALREADY_EXISITS: 'Email already exists.',
};

export { ERROR_MESSAGES };
